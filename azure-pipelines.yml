# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  name: Default
  demands:
  - 'agent.os -equals linux, Agent.Version -gtVersion 2.163.1'

steps:
- script: |
    echo "Compiling the code..."
    # Coloque aqui os comandos para compilar o seu código
  displayName: 'Compilar o código'

